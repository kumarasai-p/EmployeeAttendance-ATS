# 📦 Dependencies
# Ignores all 'node_modules' directories, whether at the root, frontend/, or backend/.
node_modules/
.pnp
.pnp.js

# 🔑 Environment Variables (CRITICAL for security)
# Ignores all .env files recursively, including .env at the root, backend/.env, and frontend/.env.
.env
.env.local
.env.*.local
*.env
# Ensure you do NOT ignore example files if you have them:
!.env.example

# 🏗️ Build Outputs
# Ignores all common build/distribution folders.
dist/
build/
# Explicitly ignores the frontend build outputs (if needed, though the above rules should cover them)
frontend/dist/
frontend/build/

# 📜 Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# 💻 OS Files
.DS_Store
Thumbs.db

# ⚙️ IDE/Editor
# Ignores configuration files and temporary files generated by IDEs (VS Code, IntelliJ/WebStorm, Vim).
.vscode/
.idea/
*.swp
*.swo